version: 0.2

phases:
  install:
    commands:
      - pip install -r requirements.txt

  pre_build:
    commands:
      - echo "Running tests..."
      - pytest

  build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
